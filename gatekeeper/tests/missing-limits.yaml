apiVersion: v1
kind: Pod
metadata:
  name: policy-test-missing-limits
  namespace: demo
  labels:
    owner: lab
spec:
  restartPolicy: Never
  securityContext:
    runAsNonRoot: true
  containers:
  - name: nginx
    image: nginx:1.27.5
