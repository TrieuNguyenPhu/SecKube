apiVersion: v1
kind: Pod
metadata:
  name: policy-test-latest
  namespace: demo
  labels:
    owner: lab
spec:
  restartPolicy: Never
  securityContext:
    runAsNonRoot: true
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
