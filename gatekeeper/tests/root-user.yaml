apiVersion: v1
kind: Pod
metadata:
  name: policy-test-root-user
  namespace: demo
  labels:
    owner: lab
spec:
  restartPolicy: Never
  securityContext:
    runAsUser: 0
  containers:
  - name: nginx
    image: nginx:1.27.5
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
