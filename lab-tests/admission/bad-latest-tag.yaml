apiVersion: v1
kind: Pod
metadata:
  name: bad-latest-tag
  namespace: demo
spec:
  securityContext:
    runAsNonRoot: true
    runAsUser: 10001
    runAsGroup: 10001
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
